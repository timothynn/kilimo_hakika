[pytest]
# `pythonpath = .` puts the backend root on sys.path so tests can import
# `main` and `app` the same way uvicorn does, without an editable install.
pythonpath = .
testpaths = tests
addopts = -q
